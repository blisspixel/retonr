# Milestone 0.1 refinement record

## Status

Status: technical review complete. The complete retained matrix passed for
implementation closure revision `b2d41fc`, and exact-main closure evidence passed at
`40643c0`. Milestone 0.1 technical work is complete. ADRs remain revisable during
0.x, and their proposed status does not block reversible 0.2 implementation.
Qualification of an exact runtime and artifact under OS-enforced non-loopback
isolation belongs to milestone 0.2.

Scope reviewed: deterministic contracts, plain-text adapter, common engine,
candidate-check service and CLI, evaluation and baseline harness, model and inference
contracts, native Ollama adapter, and grounded application path.

## Pass 1: correctness and boundaries

Completed checks:

- Separated immutable artifact facts, qualification evidence, activation decisions,
  and active bindings.
- Added a backend-neutral object-safe inference port and deterministic fake.
- Separated observed source bytes, qualified source capacity, and complete serialized
  prompt capacity.
- Kept grounded generation outside acceptance and document application.
- Routed grounded candidates through the common protection, structure, safety,
  semantic, and ranking path.
- Preserved the exact original on deterministic abstention and cancellation.

Findings corrected:

- The first baseline and Ollama modules exceeded the source-file size policy. Tests
  and response, contract, and transport responsibilities were split into cohesive
  modules.
- IPv6 loopback parsing initially rejected the bracketed URL form and was corrected.
- Ollama digests were normalized from the documented `sha256:` wire representation
  to the canonical internal digest type.
- Prompt input length had been conflated with source length and was separated before
  broader use.

Deferred by milestone scope:

- Durable generation provenance is not yet linked into the versioned transaction
  record.
- The current semantic evaluator is intentionally literal-only. Broader modes remain
  uncertain.

## Pass 2: adversarial and cross-platform

Completed checks:

- Endpoint parsing rejects hostnames, non-loopback addresses, TLS URLs, credentials,
  query strings, fragments, and non-root paths.
- The HTTP client is configured with reqwest 0.13.4 `no_proxy()`, redirect following
  disabled, automatic retries disabled, referrers disabled, and HTTP/1 only.
- Discovery, generation, digest drift, cancellation, structured candidate parsing,
  and response bounds have deterministic tests.
- Redirect, disconnect, truncated body, oversized body, wrong content type,
  preflight deadline, in-flight body deadline, exact generation parameters, and
  post-generation digest drift have deterministic loopback transport tests.
- A process-isolated proxy-canary test independently exercises the upper- and
  lower-case HTTP and all-proxy variables. A default reqwest client must reach the
  armed canary, while the Ollama adapter must contact only its configured loopback
  fake backend. The child environment removes both proxy-exclusion variables and
  inherited CGI state.
- The quality workflow includes an Ubuntu job that compiles the Ollama adapter tests
  before entering an OS network namespace, brings up loopback as its only interface,
  confirms both main route tables are empty, confirms a non-loopback connection
  fails, and then executes the compiled test binaries.
- Concurrency fixtures exercise cloned adapter handles and prove that operations are
  admitted up to the configured limit, excess work remains queued, queued
  cancellation and deadlines perform no HTTP work, and a permit is recovered after
  an in-flight transport failure.
- Workspace properties, process tests, and repository policy checks pass locally on
  Windows.

Completed evidence and scoped deferral:

- Main workflow run
  [`31658435581`](https://github.com/blisspixel/retonr/actions/runs/31658435581)
  passed for implementation closure revision `b2d41fc`, including Windows, macOS,
  Linux, repository policy, Markdown, coverage, supply chain, fuzz smoke,
  proxy-canary conformance, concurrency conformance, and the Ubuntu loopback-only
  network-namespace job.
- Dynamic review run
  [`31658436624`](https://github.com/blisspixel/retonr/actions/runs/31658436624)
  also passed for that exact revision.
- During milestone 0.2 qualification, apply OS-enforced non-loopback isolation to
  every participating Retonr and runtime process and qualify one exact runtime and
  artifact. The 0.1 fake-server suite does not establish runtime isolation,
  authenticated listener identity, or model-backed offline operation.

## Pass 3: security, privacy, accessibility, and licenses

Completed checks:

- Default inference and grounded errors use stable redacted codes.
- Grounded traces retain digests and exact component identities without raw source,
  candidate, style, prompt payload, or protected surfaces.
- Protected original surfaces are omitted from model input.
- The generation model receives no tool, filesystem, profile-mutation, or product
  authorization capability.
- Proposed data and user-research governance documents now define authorization,
  consent, adjudication, revocation, retention, and scoped deletion.

Scoped deferrals:

- Governance documents remain proposed. No non-synthetic collection is authorized;
  consent materials, named reviewers, and study-specific preregistration remain
  required before any such work.
- Exact model and runtime licenses require artifact-specific review.
- CLI terminal-safety and accessible diff work remain in milestone 0.2.
- No user interface exists yet, so desktop accessibility evidence is not applicable
  to this milestone.

## Pass 4: simplification and release readiness

Completed checks:

- Internal crates use namespace-neutral `rewrite-*` names while the private codename
  remains isolated to presentation surfaces.
- Source files are below the enforced 500 effective-line limit.
- No package publication or unsupported capability claim has been added. The README
  includes one Linux-first capture of the already implemented model-free `check`
  command under the repository screenshot policy.
- Strict Clippy, 112 passing tests, and repository policy pass after the current
  implementation tranche. One proxy child helper remains intentionally ignored by
  the ordinary test runner and is exercised by its process-isolated parent test.
- Workspace line coverage is 90.49 percent, with the grounded and Ollama paths above
  the 80 percent project floor.
- Rust documentation with warnings denied, dependency and license policy, RustSec
  audit, Markdown lint, release build, and stable fuzz-target checking pass.

Scoped deferral:

- Refine ADRs 0001 and 0003 through 0005 as implementation evidence develops. Their
  preview status is not a compatibility freeze or an implementation blocker.
- Capture model-backed rewrite, abstention, diff, and trace screenshots only after
  those exact release-build behaviors exist. The candidate-check capture does not
  imply them.

## Completion result

The four technical passes have no undispositioned critical defect. Milestone 0.1
technical work is complete with no exception implied or accepted. Reversible 0.2
implementation may proceed while preview decisions continue to mature.
